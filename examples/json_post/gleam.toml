name = "json_post"
version = "0.1.0"
description = "Buildable JSON POST example for Gluegun"
licences = ["MIT"]
target = "erlang"

[dependencies]
gleam_stdlib = ">= 0.48.0 and < 2.0.0"
gluegun = { path = "../.." }
